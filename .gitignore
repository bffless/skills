node_modules/

# plugin.json lives only in the npm package, not in the git repo.
# Having both plugin.json and marketplace.json in .claude-plugin/
# causes Claude Code to recurse infinitely.
.claude-plugin/plugin.json
